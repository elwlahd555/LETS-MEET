<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.anjanda.letsmeet.repository.mapper.AlarmMapper">
	<insert id="insertAlarm" parameterType="string">
		INSERT INTO alarm
			(a_u_no, 
			a_title, a_content, a_time, a_image)
		VALUES
			((select u_no from user where u_email = #{uEmail}),
			#{aTitle}, #{aContent}, #{aTime}, #{aImage})
	</insert>
	
	<select id="selectAlarm" parameterType="string">
		SELECT *
		  FROM alarm
		 WHERE a_u_no = (select u_no from user where u_email = #{uEmail})
	</select>

	<delete id="deleteAlarm">
		DELETE
		  FROM alarm
		 WHERE a_no = #{aNo}
	</delete>
</mapper>

